(ns lab1.core-test
  (:require [clojure.test :refer [deftest is testing]]
            [lab1.euler13-test]
            [lab1.euler17-test]))

(deftest all-tests
  (testing "Запуск всех тестов"
    (is true)))